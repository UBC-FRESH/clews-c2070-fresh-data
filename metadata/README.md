# Metadata

Use this directory for data-repository-local metadata, checksums, acquisition
manifests, and file inventories that need to travel with the DataLad dataset.

The parent repository keeps the planning/source registry in:

- `metadata/source-inventory.csv`
- `metadata/required-datasets.yaml`
